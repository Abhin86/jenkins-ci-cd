apiVersion: v1
kind: ConfigMap
metadata:
  name: {{ .Chart.Name }}-config
data:
  DATABASE_HOST: "{{ .Values.mysql.host }}"
  DATABASE_PORT: "{{ .Values.mysql.port }}"
  DATABASE_USER: "{{ .Values.mysql.username }}"
  DATABASE_PASSWORD: "{{ .Values.mysql.password }}"
  DATABASE_NAME: "{{ .Values.mysql.dbname }}"
